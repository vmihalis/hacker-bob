node_modules/
testing/**/node_modules/
site/dist/
CODEX_VISUALIZATION/dist/
CLAUDE_VISUALIZATION/dist/
__pycache__/
*.pyc
.DS_Store
*.log
*.tsbuildinfo
.env
.env.local
.mcp.json
!adapters/codex/hacker-bob/.mcp.json
.codex/
.kimi/
.agents/
.hacker-bob/VERSION
.hacker-bob/install.json
.hacker-bob/verification-rounds/
.claude/bob/VERSION
.claude/bob/install.json
.claude/bob/egress-profiles.json
# act local testing — secrets and log files
.secrets
test/act-run.log
test/act-mock-api.log
# offensive arsenal image build staging (host-fetched binaries; never committed)
docker/offensive-context/
# operator-minted image digest — deployment-specific (points at the operator's registry push), so it is
# NOT committed/shipped; install.js copies the local file into the runtime. Each operator mints their own.
mcp/lib/offensive-image.json

# Local issue drafts and planning artifacts (not framework source)
ISSUE-DRAFT-installer-hoisting.md
bob-evaluate-feedback-consolidated.md
