name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.3

      - name: Setup Node
        uses: actions/setup-node@v6.4.0
        with:
          node-version: 20
          cache: npm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm ci

      - name: Test
        run: npm test

      - name: Release check
        run: npm run release:check -- --registry --allow-published
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Dependency freshness
        run: npm run release:check:dependencies

      - name: Publish canonical package
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          if npm view "hacker-bob@$VERSION" version >/dev/null 2>&1; then
            echo "hacker-bob@$VERSION is already published; skipping."
          else
            npm publish --provenance --access public
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish Claude wrapper package
        working-directory: packages/hacker-bob-cc
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          if npm view "hacker-bob-cc@$VERSION" version >/dev/null 2>&1; then
            echo "hacker-bob-cc@$VERSION is already published; skipping."
          else
            npm publish --provenance --access public
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish Codex wrapper package
        working-directory: packages/hacker-bob-codex
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          if npm view "hacker-bob-codex@$VERSION" version >/dev/null 2>&1; then
            echo "hacker-bob-codex@$VERSION is already published; skipping."
          else
            npm publish --provenance --access public
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish Kimi wrapper package
        working-directory: packages/hacker-bob-kimi
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          if npm view "hacker-bob-kimi@$VERSION" version >/dev/null 2>&1; then
            echo "hacker-bob-kimi@$VERSION is already published; skipping."
          else
            npm publish --provenance --access public
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
