"use strict";

// Keep the optional provider's package-local conformance suite in the root
// security-test manifest until the provider becomes an independently executed
// workspace package.
require("../packages/bob-instrument-chameleon/test/codec.test.js");
