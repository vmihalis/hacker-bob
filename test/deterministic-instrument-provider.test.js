"use strict";

// Keep the optional provider's package-local conformance suite in the root
// security-test manifest until it becomes an independently executed workspace.
require("../packages/bob-instrument-deterministic/test/provider.test.js");
