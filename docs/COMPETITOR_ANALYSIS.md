# Hacker Bob — Competitive Analysis & Adaptation Backlog

*Scope: synthesis of Bob's profile, three adjacent projects (raptor, CyberStrikeAI, poc-lab), three head-to-head comparisons, and an adversarial skeptic pass that checked every candidate against Bob's live `main`. Paths below are repo-relative to match Bob's own conventions.*

> **Editor's correction (orchestrator, verified 2026-06-06 UTC).** An earlier draft of this document carried a disclaimer claiming the three external repos were "not present anywhere on this machine" and that all external-tool claims were unverified. **That disclaimer was wrong** — it resulted from a home-directory search while the source clones were under a temporary comparison workspace. The Phase-1 reader agents *did* read the real source. The external revisions checked were `gadievron/raptor@4a1fae590cd593ef464d058d2b3a8da7de07d4fd`, `Ed1s0nZ/CyberStrikeAI@c2f8ebc7430569c7ea65ace71cbe7033018ed4a8`, and `Unclecheng-li/poc-lab@53724519e0e1690557cd4dcce58085248b7589b4`. The orchestrator then **independently re-verified every load-bearing external fact** against those live clones:
>
> | Claim | Verified reality |
> |---|---|
> | raptor `core/smt_solver/csem.py`, `core/security/prompt_envelope.py` | both exist |
> | raptor ~300K non-test Python LOC | 300,210 (real; may include vendored code) |
> | raptor `test_*.py` count | 916 |
> | raptor static-analysis layer | 31 `.ql` + 4 `.qll` + 40 `.cocci`; `z3` imported in 16 files; DWARF/reachability in 312 files |
> | raptor `raptor_agentic.py` | 133,894 bytes |
> | CyberStrikeAI `internal/security/executor.go`, `internal/handler/task_event_bus.go` (TaskEventBus) | both exist; 74,133 Go LOC |
> | poc-lab structure | 16 top dirs, 1 squashed commit, 6 committed ELF binaries |
>
> Conclusion: the external analysis is **trustworthy at the load-bearing level**. Fine-grained micro-claims (exact behavior of a specific function, precise sub-metrics) are worth a spot-check before you act on them, but the "these tools might be entirely hallucinated" framing is **withdrawn**. All Bob-side claims were already checked against the live tree with file cites. The inline tags from the draft have been removed as systematically over-cautious.

## TL;DR (highest-signal takeaways)

1. **None of the three is a direct competitor to Bob's center of gravity.** raptor owns source/binary/SCA but its live-web side is reportedly a stub; CyberStrikeAI is a red-team console that reportedly makes verification and scope-enforcement *non-goals*; poc-lab is a knowledge corpus with no agent at all. Bob is alone in doing live-web bounty + 6-chain SC audit + OSS source audit through one verified, scope-enforced pipeline.

2. **Bob's #1 acknowledged limit is a dropped producer, not a missing feature.** The OSS reachability/severity-ceiling classifier exists as code at blob `d1647c0` (with `1a456f1`, `9433537`), but it is **not in Bob's working tree on `main`**: `mcp/lib/reachability.js` does not exist and `grep -ci reachab mcp/lib/repo-target.js` = 0. The **entire consumer half is still wired and orphaned** — `assignment-brief.js` (5 refs, with the comment "Stamped … by repo-target.js"), `wave-brief-derivation.js` (4 refs), `dashboard.js` (4 refs), and the evaluator prompt (`prompts/roles/evaluator.md:65-66`) instructs agents on `severity_ceiling`/`attack_vector`/`network_reachable`/`network_reachable_anchors[]`/`residual_hunt_targets[]` fields that nothing populates. The guard tests (`test/repo-target.test.js`, `test/dashboard.test.js`) are absent from `test/mcp-test-manifest.json` and `require` renamed modules, so the regression went uncaught. **The fix is "restore your own code," not "adopt raptor."** Highest value-to-effort move in the backlog. (Precise framing: see gap #1 — there is *no removal commit*; the producer was superseded at a merge, not deleted from a live `main`.)

3. **The skeptic pass demoted or killed roughly half the candidate list** — 6 candidates are *already-covered* (record-time provenance, screenshot hygiene, in-wave steering, model-routing table, exploitation-completeness label, dependency vuln-DB) and several "high value" claims were overstated. It was not perfect, though: it *itself* propagated one error (it listed "reachability demotion" as a live pre-LLM triage input — it is not; see TL;DR #5 and the inconsistency note in the gaps section). Net, believing the *raw* candidate list would still have caused real wasted work.

4. **Several proposed "improvements" are actively dangerous for Bob's mission.** SMT-unsat-skip and "dependency-not-in-CVE-DB-skip" both invert Bob's enforced no-premature-termination doctrine and would suppress the novel 0-days Bob exists to find. A cheap-tier FP prefilter *before* the brutalist verifier inverts freeze-then-verify. Adopt the *signals*, never the *auto-kill gates*.

5. ** raptor's real lesson is economic, not feature-level: it reportedly spends serious effort NOT calling the LLM** (free deterministic refutation, SMT pruning, dead-code suppression, Wilson-bounded prefilter). Bob feeds essentially everything to LLM evaluators and trusts LLM judgment for grade *quality* (only the arithmetic is server-checked). Bob already has *some* pre-LLM triage — `lead-scoring.js` (`shouldPromoteLead`, default `min_score:60`, `limit:8`) and per-surface context budgets — though less than the candidate claimed, and notably **NOT** reachability-based (the stamp is absent, and even when present it never feeds ranking/budgeting; see the inconsistency note). What Bob lacks is *measuring and surfacing* the savings/reliability.

6. **poc-lab's real lesson is disclosure craft.** Bob runs an active GHSA/CVE campaign and the operator hand-transcribes `report.md` into advisory forms, yet Bob's OSS report branch omits even a CWE line (`prompts/roles/reporter.md:28-31` — the HTTP branch at `:38` and the SC branch at `:49` both have CWE). Cheap prompt-only wins live here.

7. **Bob's genuine, novel gaps after the skeptic pass are narrower and cheaper than the architecture docs imply:** restore reachability stamping (and add a consumer that *acts* on it); add a patched-vs-unpatched differential oracle (kills the recurring "this was already fixed" maintainer pushback on residual findings); quarantine untrusted text entering briefs; enrich the OSS report template; broaden root-cause vocabulary beyond memory-safety (two genuinely-absent families, not four); source fuzz seeds. Everything else is park/defer.

## Honest verdict per project

> **Note.** "What it is" / "What X does better" content below was read from the real clones and the load-bearing facts were re-verified (see editor's correction above). "What Bob does better" halves are Bob-side and were checked against the live tree.

### raptor vs Bob

**What it is :** A heavyweight Claude-Code-native source/binary/supply-chain analysis engine (reported "~300K non-test Python LOC / ~916 `test_*.py`" — implausibly large; likely counts vendored/generated code) chaining Semgrep+CodeQL+Coccinelle, AFL++/libFuzzer, Z3/SMT, DWARF+nm reachability, and SCA→SBOM/VEX, with a deliberately deterministic, scanner-driven macro pipeline.

**Peer/threat/inspiration:** A **serious peer in a different room** and Bob's richest inspiration source. Reportedly not a market threat — its `/web` is described as an alpha stub (empty SSRF rules, uninitialized offsec submodule) and it has zero smart-contract capability, so the only true head-to-head surface is OSS source auditing, where raptor is said to be materially deeper. 

**What raptor does better :** Pre-LLM deterministic refutation economics (free gates + savings counters); SMT/Z3 path-feasibility with real C semantics; binary-oracle dead-code suppression with held-out generalization numbers; statistically-honest self-calibration (Wilson bounds, per-`(model,decision_class)` trust cells); agent-self-security (prompt-injection nonce envelope, x-source provenance, sandbox); headless CI mode; CWE-strategy YAML playbooks with a scoring picker; self-growing verified-outcome corpus.

**What Bob does better (Bob-side, verified):** Genuinely exploratory, strategy-forming orchestration; MCP-owned content-addressed event-sourced state authority; live-web + 6-chain SC engines; live-target scope/SSRF/PII safety; 4-host portability with `--check` drift gates.

### CyberStrikeAI vs Bob

**What it is :** A self-hosted Go monolith red-team console — reported ~89 CLI-tool recipes + built-in C2 + webshell manager + host PTY behind an Eino-ADK agent loop, driven conversationally through a vanilla-JS SPA. Operator-trust + access-control posture; finding-correctness and scope-enforcement are described as explicit non-goals.

**Peer/threat/inspiration:** A **peer in runtime/UX engineering, a different category in mission.** Reportedly what Bob would be if you deleted the correctness layer and bolted on a C2. Strong inspiration for observability/UX; near-zero inspiration for correctness.

**What CSAI does better :** Live reconnectable in-run observability (`TaskEventBus` SSE timeline, refresh/multi-tab re-attach mid-run); in-loop HITL steering at tool granularity; runtime context-compaction middleware for long single-agent loops; full kill-chain breadth incl. post-exploitation; swappable orchestration topologies + checkpoint/resume; generic third-party MCP federation; broad integration surface (Burp/DingTalk/Lark/WeChat).

**What Bob does better (Bob-side, verified):** Essentially everything correctness- and safety-related — freeze→3-round adversarial verify→code-adjudicated→5-axis grade vs (reported) pure-CRUD `record_vulnerability`; scope enforcement + SSRF hardening vs (reported) zero target-boundary enforcement; sandboxed runners; registry-driven single-source-of-truth vs (reported) copy-pasted prompt drift; PII guardrails.

**Caution :** CSAI's RAG is reportedly an anti-pattern (sophisticated indexer over a 2-category corpus shipped `knowledge.enabled: false`, brute-force O(N) cosine) — do not copy.

### poc-lab vs Bob

**What it is :** A reported 16-directory, single-squashed-commit human-curated corpus of working exploit PoCs for mostly-2026 high-severity CVEs, each with a deep bilingual root-cause write-up. No agent, orchestration, automation, or machine-readable layer.

**Peer/threat/inspiration:** **Not a peer — a knowledge artifact, and a donor of discipline.** Reportedly ~60% of its content (kernel LPE, browser UAF, binary pwn) is outside Bob's scope; the thin overlap is C-service/parser root-cause clarity (Redis RESTORE, NGINX Rift, Valkey RESP).

**What poc-lab does better :** End-to-end exploitation depth demonstrating HIGH/CRITICAL (vs Bob stopping at "reachable bug + sanitizer repro"); cross-CVE family synthesis; disclosure-grade documentation (CVSS vector, per-distro matrices, fix-commit, timeline, references); reusable known-trigger seed vectors; intellectual honesty as a first-class field (trigger-vs-full-exploit labels).

**What Bob does better (Bob-side, verified):** It is an actual system with verification rigor, safety architecture, breadth, and reproducible machine-readable state. (Note: poc-lab reportedly ships untrusted committed ELF binaries and weaponizes real system files — exactly the "harvest real secrets to confirm" anti-pattern Bob's own rules forbid; but consistent with the read-only handling guidance regardless.)

## Bob's real gaps (ranked by impact) — skeptic-confirmed only

| # | Gap | Severity | Confirmed by |
|---|-----|----------|-------------|
| 1 | **OSS reachability/severity-ceiling stamping has no producer in `main`'s working tree** — consumer plumbing (briefs, dashboard, evaluator prompts) wired to fields no producer writes; `mcp/lib/reachability.js` doesn't exist; `grep -ci reachab mcp/lib/repo-target.js` = 0; guard tests orphaned from the manifest. **Important precision:** there is *no removal commit*. `d1647c0`/`1a456f1`/`9433537` are ancestors of HEAD, but `d1647c0` is not on `main`'s first-parent line and `git log --all -S detectNetworkReachability` returns only the *add*. The producer shipped on a side branch whose `repo-target.js` was **superseded by the parallel "Plane-O" rewrite (`9432777`→`2791a81`) at the v2.1 merge** — the consumers/prompts survived the merge, the producer did not. So "live on main, then deleted" is wrong; "ancestor blob exists, never present in main's working tree" is right. The blob is re-portable, so the recommendation stands. | HIGH | own-code pass (commit ancestry + blob existence) |
| 2 | **No patched-vs-unpatched differential oracle in the live pipeline** — exists only as benchmark scoring doctrine; no verifier rebuilds a fixed/clean commit, so residual findings can't prove "fix present, bug still fires" | MED-HIGH (for residual/self-patch disclosures) | poc-lab pass for the donor lesson; the Bob-side absence is verified |
| 3 | **No untrusted-content quarantine into agent briefs** — `audit_summary`/`schema_slice`/`intel_hints`/`traffic_summary`/`surface_graph_slice` + resolved bodies enter prompts with secret-redaction + size-cap (`sensitive-material.js`, 4000-char cap) but **no "this is data, not instructions" fence** (0 datamark hits; `audit-report-parser.js` persists description/recommendation/summary verbatim); Bob's own `docs/LLM_AGENT_SECURITY_LANDSCAPE_2026.md` names this a CVE-bearing risk | MED | raptor pass (the donor idea ; the Bob-side absence is verified) |
| 4 | **OSS/CVE report template is too thin for disclosure** — `mcp/lib/tools/compose-report.js` `SECTION_KINDS` = `[impact,repro,evidence,severity,remediation,chain_summary,provenance]` (closed enum, no CVSS/refs); OSS reporter branch lacks even a CWE line (`reporter.md:28-31`) while web (`:38`) and SC (`:49`) branches have one | MED | poc-lab pass for the donor lesson; the Bob-side absence is verified |
| 5 | **Root-cause vocabulary is memory-safety-heavy — but only *two* families are genuinely absent, not four.** Confirmed absent **as vulnerability families** (0 hits in the bug-class sense): **MAC-then-decrypt / crypto-ordering** and **validate-vs-consume / parse-then-validate**. The previously-listed "canonicalization desync" and "error-path-no-rollback" are absent **as a vulnerability family** but their bare tokens *do* appear in infrastructure code (`canonicaliz` = 12 hits, all command/value canonicalization for hashing in `claims.js`/`verification-contracts.js`; `rollback` = 15 hits, all wave-state rollback in `wave-scheduler.js`) — so phrase any claim as "absent as a vuln family," not "zero hits." **"FSM-field-not-reset-on-early-return" is NOT net-new** — see already-covered table | MED | poc-lab pass for the donor lesson; the Bob-side hit-counts are verified |
| 6 | **No curated fuzz-seed sourcing** — `recommendedCommandsFor` (`repo-env.js:207`) emits only build/test/compose roles; the `"fuzz"` role enum (`repo-env.js:141`) is never emitted, and no seed source is named; blind fuzzing plateaus (per `project_libde265_oss_review`) | MED | poc-lab pass for the donor lesson; the Bob-side absence is verified |
| 7 | **No cost/savings or "evaluator runs avoided" telemetry** — Bob does some pre-LLM triage (`lead-scoring.js shouldPromoteLead`, per-surface context budgets) but never makes the saved budget visible. **Down-rated** because one of the two intended trigger sources ("down-budget by ceiling") does not exist yet — reachability never feeds ranking/budgeting (see inconsistency note); only the `lead-scoring` filter is a real, live trigger today | LOW | raptor pass for the donor idea; Bob-side triage map is verified |
| 8 | **No live in-run observability stream** — `dashboard.js` is manual-refresh, multi-session aggregate, loopback-only (`127.0.0.1:4873`, `isLoopbackHost`), no SSE; blind during an in-flight wave (pull tools `bob_wave_status`/`bob-statusline.js` partially cover this) | LOW (polish) | CSAI pass for the donor idea; Bob-side absence is verified |
| 9 | **No measured self-reliability/calibration** — `grade-verdict-store.js` validates only grade *arithmetic* (`total_score` must equal sum/max of rubric scores, lines ~115-275); grade *quality* is fully LLM-trusted; no FP-rate, no held-out numbers | MED (but mechanism mostly dormant at Bob's claim volume) | raptor pass for the donor idea; Bob-side absence is verified |
| 10 | **Adjudicator has no agree-but-diverge trigger** — `findingDiffs` (`verification.js:471-473`) only diffs `disposition/severity/reportable`; two rounds can agree on the tuple while describing different bugs | LOW (dangerous cases already saturated by force-replay + QA sample + `roast_disagreement`) | raptor pass for the donor idea; Bob-side limitation is verified |
| 11 | **No general SMT outside EVM**; **no self-growing technique corpus**; **full 148-tool schema advertised to all hosts** (`TOOLS.length=148`; `transport.js:43` returns `result:{tools}` unconditionally on `tools/list`) | LOW (ROI/soundness/scope issues) | raptor + CSAI passes for donor ideas; Bob-side facts verified |

**Inconsistency correction (apply everywhere):** the term "reachability demotion" was used in earlier drafts as if it were a *live pre-LLM triage input*. It is not, on two counts: (a) the producer is absent (gap #1), so the field is always null; and (b) **even when stamped, nothing ranks, budgets, demotes, or promotes on it** — `severity_ceiling`/`network_reachable`/`reachab*` have **zero** references in `surface-router.js`, `lead-scoring.js`, `wave-planner.js`, `ranking.js`, and `lead-promotion.js`; they appear only in `assignment-brief.js`, `wave-brief-derivation.js`, and `dashboard.js`, all of which **forward or display** the field. So reachability triage is "wired but inert until #1 is restored, **and even then is advisory-to-brief only, not a ranking/budget gate**." The only live pre-LLM triage mechanisms are `lead-scoring.js` (`shouldPromoteLead`, default `min_score:60`, `limit:8`) and per-surface context budgets.

**Note on #1:** restoring the stamp re-arms the evaluator's severity-ceiling discipline but does **not** by itself lift findings to HIGH/CRITICAL. Per `project_bob_oss_severity_ceiling_diagnosis` the MED/LOW ceiling is multi-causal (no grade-time acceptance gate, chains can't elevate severity, record-time proof gate downgrades on harness block, no compiler-rt, OOM cap). And per the inconsistency correction above, the restored stamp is *advisory-to-brief only* — closing the ceiling requires both the producer **and** a new consumer that acts on it (the grade-time gate in Big Bet #1). Reachability is necessary, not sufficient.

## Adaptation backlog (ordered by value-to-effort)

QW = quick win. Effort/value are **skeptic-refined**, not the original candidate's claims. "Adopt" reflects the refined scope. External-source donor ideas are ; the Bob-side integration points are verified.

| Rank | ID | Source | What to adopt (refined) | Effort | Value | QW | Integration point + how |
|------|-----|--------|------------------------|--------|-------|----|------------------------|
| 1 | `reachability-restore` | own code (raptor for the optional precision idea) | **Restore Bob's dropped reachability/severity-ceiling producer**; borrow only raptor's absence-of-dataflow idea as optional precision when a build/CodeQL exists | M | **HIGH** | — | New `mcp/lib/reachability.js` called from `buildRepoInventory`/`buildInventoryProjection` in `mcp/lib/repo-target.js`; re-port from blob `d1647c0` (+`1a456f1`) — it has no removal commit but is a recoverable ancestor blob; re-add `test/repo-target.test.js`+`dashboard.test.js` to `test/mcp-test-manifest.json` and fix stale imports (`hunter-completion.js`→`agent-run-completion.js`, `finding-store.js`→`claims.js`). Consumers need **no change** — already wired (but are display/forward only; see #1a). |
| 1a | `reachability-consumer` (companion) | own design | **A consumer that acts on the stamp** — without it, restoring the producer only fills brief/dashboard text. Add a grade-time acceptance/lift gate (see Big Bet #1) | S | HIGH (paired) | — | `mcp/lib/grade-verdict-store.js` / `lifecycle-gates.js`: gate that lifts AV:N findings above MEDIUM or suppresses AV:L "ceiling" inflation. This is the only place reachability changes output today. |
| 2 | `oss-disclosure-template` | poc-lab | Add **CWE line + suggested CVSS-4.0 vector + references** to OSS report findings only (skip per-distro matrix / fix_commit / timeline / mermaid) | S | MED | **QW** | Prompt-only: OSS branch of `prompts/roles/reporter.md` (~28-31) + mirror `.claude/agents/report-writer.md`, gated `capability_pack.startsWith('oss_')`. CVSS assembled from final-verifier severity + `attack_vector` (AV:N when `network_reachable`, else AV:L) — labeled "suggested." Zero `mcp/lib/tools/compose-report.js` change. |
| 3 | `fuzz-seed-lens` | poc-lab | **Prompt-only seed-sourcing lens** (prioritize target's own `testdata/` + existing OSS-Fuzz `*_seed_corpus`); reject poc-lab's Redis/RESP/HPACK corpus (doesn't intersect any Bob OSS finding) | S | MED | **QW** | Add bullet to `fuzz_run`/`oss_native_code` lens in `prompts/roles/evaluator.md` (~64); optionally emit the unused `fuzz` role from `recommendedCommandsFor` C/C++ branch in `mcp/lib/repo-env.js`. Any static seeds → scope to codec families Bob hits (HEVC/EXR/DWG/DICOM/TIFF/JPEG/PNG/NFS-XDR), under `.hacker-bob/knowledge/`. |
| 4 | `differential-oracle` | poc-lab | **Optional patched-vs-unpatched differential** for high/critical `oss_native_code`; 3 control kinds: `upstream_fix` (residual proof), `self_patch` (patch validation), `pre_introduction` (regression proof). Adopt the *idea only* — never secret-dump | M (M-L full) | HIGH | — | Reuse the `bob_repo_docker_run` writable path: a `compose`-role command does `git checkout <ref>`/`git apply <patch>` inside the container, rebuilds, reruns repro. **Shallow-clone precondition (must flag):** `self_patch` works regardless (local patch, no history; network-none is fine). But `upstream_fix`/`pre_introduction` need the target object in `.git` history — if `bob_init_repo_session` shallow-clones or `/src` lacks `.git`, `git checkout <fix_ref>`/`<parent_ref>` fails object-missing and those two controls **degrade to blocked**. The C/C++ recipe at `repo-env.js:~314` does `cp -a /src/. /work/repo/`, so `.git` carries *if it exists* — **verify init clone depth and require a full (non-shallow) clone for upstream_fix/pre_introduction.** Add a differential evidence row `{vuln_run_id, vuln_exit_code, control_run_id, control_exit_code, control_kind, verdict}` to `mcp/lib/tools/write-evidence-packs.js`+`mcp/lib/evidence.js`; optional verifier step; small `grader.md` proof_quality bonus. x86_64-preferred, optional (not a gate). |
| 5 | `root-cause-families` | poc-lab | Add **exactly 2 net-new non-memory-safety families** (MAC-then-decrypt / crypto-ordering; validate-vs-consume / parse-then-validate). **Plus** a worked *refinement* of the existing "state-machine confusion" family (e.g. FSM-field-not-reset-on-early-return) — but as elaboration of an existing entry, not a new family. Skip canonicalization-desync / error-path-no-rollback unless an actual finding motivates them | M (content-only) | MED | — | Fold into the `oss_native_code` pack summary in `mcp/lib/technique-packs.js:92` (which already names "state-machine confusion") + `oss-native-code-protocol-memory` entry in `.hacker-bob/knowledge/evaluator-techniques.json`; express witnesses in the **existing** invariant+witness shape (`invariant-template-corpus.js` / `TRANSITION_KIND_CONTRACT_TEMPLATES`). **Do NOT** ship a standalone scored `oss_root_cause_families` entry — the OSS brief plumbing is unwired (`context.ossTechniquePacks` is referenced *only* at `assignment-brief.js:403` with no producer) and `bob_select_technique_packs` is web-only (`select-technique-packs.js:22` → `role_bundles: ["evaluator-web","orchestrator"]`). Improves coverage, not severity. |
| 6 | `injection-envelope` | raptor | **Per-call-nonce datamark envelope** around untrust-bearing brief slices + body-resolver output + one standing agent instruction; skip raptor's per-model profiles & heavy slot-discipline | M | MED | — | New `mcp/lib/untrusted-envelope.js` (`crypto.randomBytes` nonce, like `session-cap.js`); wrap only `traffic_summary`/`audit_summary`/`intel_hints`/`schema_slice`/`surface_graph_slice` in `assignment-brief.js` and stored bodies in the body-resolver path (`resolve-body.js`); one preamble line in role/system prompts. Keep existing size-cap + secret redaction. Defense-in-depth, not a boundary. |
| 7 | `savings-telemetry` | raptor | **Savings-visibility sliver only** — emit a counter when a lead is filtered (`shouldPromoteLead`). **The "down-budget by ceiling" trigger is aspirational** (depends on #1 + #1a landing first). **Reject** the SMT-unsat / not-in-CVE-DB auto-skips (unsound for a 0-day tool) | S-M | LOW | — | New `evaluator_run_avoided` event via `mcp/lib/pipeline-events.js` (live reasons today: `low_score`/`over_promotion_limit`; add `below_ceiling` only once reachability acts on ranking), aggregated in `mcp/lib/pipeline-analytics.js`. **Descriptive only, never a hard gate.** (Planner is `wave-planner.js`+`lead-promotion.js`, not `assignments.js`.) |
| 8 | `live-sse-tail` | CSAI | New **per-session SSE tail route** on the existing dashboard server (file-backed, not a Go event-bus port) | S-M | HIGH-but-low-priority | — | New `/api/session/:domain/events` (`text/event-stream`) in `mcp/lib/dashboard.js` `routeDashboardRequest`; incremental byte-offset/`event_id` cursor over `frontier-events.jsonl`; resume via `Last-Event-ID`; **must also merge** `verification-round-store`+`wave-handoff-store` (separate ledgers). Loopback-only; run payloads through `capArray`/redaction. Do only when the parallel-run UX itch is real. |
| 9 | `calibration-observability` | raptor | The **measured-calibration kernel only** — passive, default-off, sample-floored per-`(model,attack_class)` outcome ledger + Wilson read tool + **non-binding** grader prior. Drop the cheap-tier prefilter and any auto-kill | L | MED (mostly dormant) | — | On `bob_write_grade_verdict`, persist verdict/verification disposition labels to a cross-session JSONL (model after `pack-telemetry.js`). Prereq: needs the parked I6 cross-session store first. **Never short-circuit a claim before the brutalist verifier** (inverts freeze-then-verify). |

### Defer / park (real gaps, but ROI or soundness says wait)

| ID | Source | Why parked | If ever revisited |
|----|--------|-----------|-------------------|
| `verified-outcome-exemplars` | raptor | Corpus too sparse to "sharpen with use"; grade store holds only rubric scores not technique content (cross-session read barred by hard rule); anchoring risk vs novel-high-sev O-1 strategy | Feed only sanitized rubric-distribution into the **grader** (cross-session severity consistency), not the evaluator hunt brief |
| `semantic-entropy-adjudicator` | raptor | Dangerous cases already saturated (force-replay all agreed high/critical, `<=5` reportable replay-all, QA sample, `roast_disagreement`); Jaccard-over-prose would fire on boilerplate → replay storms | Only the deterministic `artifact_hashes`-mismatch form for findings both rounds independently replayed; wire into `computeAdjudicationPlanHash` + a new `reasoning_divergence` enum |
| `smt-feasibility-tool` | raptor | Bob's lanes prove impact with concrete witnesses (live HTTP replay / Halmos / ASAN+fuzz) that are *stronger* than an SMT sat-model; `halmos-runner.js` blocks `--solver-command`/`--smt-output`; unsat→prune = false negatives | At most port `csem.py` C-semantics overflow predicates as **corroboration-only** feasibility signals — never a prune/termination gate |
| `adapter-dynamic-tool-unlock` | CSAI | CSAI's in-process middleware can't be replicated (Bob owns neither Codex nor Kimi runtime); cost only bites non-primary hosts (Kimi parked, issue #65); 148-prefix is prompt-cache-amortizable | Env-scoped per-role `tools/list` filter using `toolNamesForRoleBundle` — but means per-worker server processes |
| `osv-scanner-pack` | raptor | Dependency vuln-DB enrichment already exists (`cve-scope-matcher` + `trivy`/`cargo-audit`/`npm-audit`/`pip-audit` packs in `cli-tool-packs.js`, fired by `dependency_observed`); known-vuln-in-dep is anti-aligned with the 0-day strategy | One-line: add `osv-scanner` to `SEED_PACKS` in `mcp/lib/cli-tool-packs.js` (it is not currently in the deferred list there, but the slot is trivial) |

## Quick wins (S effort, high/med value)

- **`oss-disclosure-template` (Rank 2):** prompt-only CWE + suggested-CVSS + references on the OSS report branch. Directly cuts the operator's hand-transcription friction on every GHSA/CVE filing. Zero schema/test churn.
- **`fuzz-seed-lens` (Rank 3):** prompt-only seed-sourcing lens. Lifts fuzz *throughput* on the exact C/C++ codec/parser targets Bob actually hits, with no corpus to maintain.
- **`reachability-restore` + `reachability-consumer` (Ranks 1/1a) are the honorary quick win:** the restore is technically M, but it is *restoring already-paid-for plumbing* (re-port a recoverable ancestor blob + re-arm dropped tests), and the paired consumer is S. Highest value-to-effort in the doc by a wide margin — and the restore alone stops the evaluator prompts from instructing agents on phantom fields *today* (the consumer is what makes it change output).
- **`savings-telemetry` (Rank 7) descriptive sliver:** small, makes the *existing* `lead-scoring` filter savings legible, feeds the existing benchmark plan. (Lower value than earlier framing — the ceiling-based savings source doesn't exist yet.)

## Big bets (L effort, high value — the strategic moves)

Honest framing: after the skeptic pass, **the highest-value work is M-effort, not L** (reachability is a producer restore). The strategic bets are *combinations and directions*, not raw effort:

1. **Close the severity ceiling end-to-end** (`reachability-restore` + `reachability-consumer`). Restoring the producer is M; the strategic completion is the *second* small change — a grade-path consumer in `mcp/lib/grade-verdict-store.js`/`lifecycle-gates.js` that actually lifts AV:N findings above MEDIUM or suppresses dead ones. **Restoring the stamp without this consumer changes nothing in the output** — the wired consumers only display/forward it. This pairing is the move that shifts Bob's OSS output distribution from MED/LOW toward HIGH. Pair with the differential oracle (#2) so a lifted HIGH is *defensible*.
2. **Disclosure-grade OSS pipeline** (`differential-oracle` + `oss-disclosure-template`). Together they make Bob's residual/incomplete-fix findings — its bread and butter (netatalk, rpcbind, libraw residuals) — both *provable* ("fix present, bug still fires") and *file-ready*. This directly rebuts the recurring "this was already fixed" maintainer pushback (openexr, libde265) and strengthens the O-1 disclosure campaign. (Mind the shallow-clone precondition on the `upstream_fix`/`pre_introduction` controls.)
3. **Agent-self-security posture** (`injection-envelope`, longer-term the calibration layer). Bob ingests attacker-controlled HTTP bodies and third-party audit reports into agent prompts (`audit-report-parser.js` persists them verbatim); raptor reportedly treats exactly these as adversarial inputs. This is genuine defense-in-depth in Bob's own threat space, even though it is mitigation, not a boundary.
4. **Measured self-reliability** (`calibration-observability`) is the only true L-effort high-strategic-value item — it would give Bob its *first* quantified reliability signal — but it is mostly dormant at single-operator claim volume, so it is a long-horizon investment, not a near-term win.

## Explicitly rejected / already-covered (prevents wasted work)

| Candidate | Verdict | Reason + Bob's existing analog (verified) |
|-----------|---------|-------------------------------|
| `state-machine / FSM-reset root-cause family` | already-covered (moved from gap #5) | The `oss_native_code` pack summary already names **"state-machine confusion"** (`mcp/lib/technique-packs.js:92`); the evaluator brief repeats it (`prompts/roles/evaluator.md:64`) and the OSS reporter branch instructs "name the parser/**state transition**" (`reporter.md:31`). The FSM/state-transition concept has ~25 hits across `mcp/ prompts/ .claude/`. FSM-field-not-reset is at most a *worked refinement* of this existing family (Rank 5), not net-new. |
| `xsource-provenance-guard` | already-covered | **O-P4** `assertNotStaticOnlyNativeHighSeverity` (`mcp/lib/claims.js:412`, called at `:447`) + `repoCommandRunRowSatisfiesEvidence` (`:394`) is exactly a record-time provenance guard for the highest-risk surface, + content-hash binding (`normalizeEvidenceReferenceShape`) + verify-time re-derivation (`claim-freeze.js`). Literal port infeasible on web — `http-records.js` stores redacted metadata only, no bodies to match. |
| `screenshot-context-hygiene` | already-covered | `browser-driver.js` `takeScreenshot` writes the PNG to disk and returns **only** `{artifact_path}` — bytes never enter context. Proposed `bob_resolve_body` routing would be a *regression* (`resolve-body.js:121` is `toString("utf8")` → base64 inflation). Real analog to `analyze_image` is `bob_browser_snapshot` (accessibility tree). |
| `in-wave-operator-steering` | already-covered | Three deterministic between-wave knobs exist: `bob_set_queue_policy.priority_order`, `bob_record_surface_leads`/`bob_promote_surface_leads`, `bob_clear_terminal_block` + `scope_exclusions`. `operator_note` is *intentionally* advisory free-text. CSAI's real value (intercept a live tool call) is structurally impossible in Bob's fork-merge wave model. |
| `reasoning-effort-routing-table` | already-covered | Model routing is already declarative + CI-enforced: `CLAUDE_ROLE_SPECS` in `scripts/lib/claude-role-renderer.js` (every role has `model:`; `balanced-verifier` = opus, `brutalist`/`final`/`grader` = sonnet — matches Opus-first/balanced-on-Opus doctrine), fanned out by `scripts/generate-claude-roles.js` with `--check` drift guard; codex/kimi adapters mirror it. The "effort" half is N/A (Claude subagents have no thinking-budget knob). |
| `exploitation-completeness-label` | already-covered | Trigger-vs-weaponized is already machine-legible: `write-verification-round.js` `disposition`/`severity`/`reportable`/`confidence_reasons`, `write-chain-attempt.js` `outcome` enum + `chain_potential` grader axis, balanced-verifier precondition-gated severity. The proposed integration (`bob_record_candidate_claim`, hunter self-report pre-verification) fights freeze-then-verify. |
| `pre-llm-refutation-gate` (skip mechanism) | already-covered + harmful | Pre-LLM triage already exists but **down-ranks / budget-caps rather than hard-skips** (`lead-scoring.js shouldPromoteLead`, `surface-router.js` budgets, `cve-scope-matcher.js`) — **note: it is *not* reachability-based** (the stamp is absent and never feeds ranking; see the inconsistency correction). The two proposed *skips* invert Bob's enforced smart-contract anti-skip doctrine (`.claude/rules/evaluating.md`) and would suppress novel findings. Only the savings *telemetry* survives (Rank 7). |
| `dependency-vuln-db-enrichment` | already-covered | Delivered twice: paste-fed NVD matcher (`bob_public_intel`/`cve-scope-matcher.js`) + per-ecosystem offline audit packs (`cli-tool-packs.js`: trivy/cargo-audit/npm-audit/pip-audit) fired by `dependency_observed` events. OSV/KEV/EPSS deltas are real but low-ROI; if anything, the one-line `osv-scanner` SEED_PACK add. |

## Architecture-level lessons

- **Static-analysis layer:** raptor reportedly reveals Bob's OSS engine is mechanically thin — `bob_static_scan` is token-contract-only, no CodeQL/Coccinelle, no SARIF dedup, and no reachability *producer* on `main` (verified). The correct response is *not* to import raptor's binary-oracle wholesale: Bob's `oss_native_code` surfaces are **source checkouts created pre-build**, and Docker builds are best-effort and frequently arch-blocked, so a compiled-artifact oracle is a poor fit at stamp time. Bob's own *source-heuristic* classifier (the dropped producer) is the right architecture; raptor's absence-of-dataflow refutation is an optional precision booster only when a build/CodeQL happens to exist.

- **Fuzzing maturity:** Bob's fuzz path is the least-finished engine (verified) — the `fuzz` recommended-command role exists (`repo-env.js:141`) but is never emitted, seeds come only from the target's own `testdata/`, and aarch64 compiler-rt/libFuzzer friction is documented. raptor's reported host-native AFL++/libFuzzer with reachability-weighted prioritization and plateau detection is the maturity target ; the cheap step toward it is seed-sourcing discipline (Rank 3), not a new harness.

- **Multi-agent patterns:** Bob's "thin, dumb coordinator + MCP-owned event-sourced state + many short-lived stateless agents" is genuinely ahead of all three (Bob-side verified; the comparison rests on notes). Bob should *not* adopt CSAI's reported runtime context-compaction middleware: Bob sidesteps long-context drift by construction (bounded briefs + `resume`). The one place Bob is reportedly behind is **economics** — the "spend effort not calling the LLM" instinct is the philosophy to internalize, expressed in Bob's idiom as *measured* savings/calibration (Ranks 7, 9), never as auto-kill gates that fight freeze-then-verify.

- **Observability/UX:** CSAI exposes Bob's weakest non-correctness axis — there is no live in-flight view (verified: only manual-refresh aggregate + pull tools). This is real but correctly low-priority; the file-backed SSE tail (Rank 8) is the Bob-idiomatic form when the parallel-run itch is felt. Bob should *not* port CSAI's reported in-memory event bus — its `frontier-events.jsonl` ledger already is the event stream.

- **Knowledge representation:** All three reportedly confirm Bob's "knowledge as scored data, not prose-in-prompts" is the right direction but under-populated. The honest constraint: per Bob's own forensic diagnosis, more technique vocabulary improves *coverage*, not the *severity ceiling* — so knowledge enrichment (Rank 5) is corroborating work, not a headline capability. And the enrichment is *smaller* than earlier framing implied — only two families are genuinely absent (crypto-ordering, validate-vs-consume); state-machine confusion is **already** in the pack summary (`technique-packs.js:92`). The structurally interesting move is that Bob already has the "named family + worked witness" format implemented for web3 (`invariant-template-corpus.js` + `TRANSITION_KIND_CONTRACT_TEMPLATES`); extending that shape to OSS is the coherent path, gated behind finishing the unwired `context.ossTechniquePacks` OSS-brief plumbing.

- **Self-defense as a first-class concern:** raptor is the clearest signal that Bob under-invests in treating its *own agents* as an attack surface. Verified Bob-side: Bob redacts secrets in outputs (`sensitive-material.js`, 4000-char cap) but does **not** quarantine attacker-controlled bytes flowing *into* prompts (`audit-report-parser.js` persists them verbatim; 0 datamark/fence hits) — and Bob's own landscape doc names this a CVE-bearing risk. The fix is modest (Rank 6) but the directional lesson is that as Bob ingests more attacker-controlled content (HTTP bodies, audit reports, schemas), prompt-injection hardening should track that growth.

## Recommended next 3 moves

1. **Restore the reachability/severity-ceiling producer AND add the consumer that acts on it** (`reachability-restore` + `reachability-consumer`, Ranks 1/1a). Re-port the recoverable `d1647c0`(+`1a456f1`) blob into a new `mcp/lib/reachability.js` called from `repo-target.js` inventory build, re-arm `test/repo-target.test.js`+`dashboard.test.js` in `test/mcp-test-manifest.json`, and add the grade-time gate in `grade-verdict-store.js`/`lifecycle-gates.js`. **The restore alone only re-fills brief/dashboard text — the wired consumers are display/forward only; the gate is what changes output.** Together they stop agents being instructed on phantom fields and become the prerequisite for lifting OSS findings above MEDIUM. Highest value-to-effort, and a regression the test suite currently can't catch. (Framing note: there is no removal commit to revert — re-port the ancestor blob.)

2. **Ship the two prompt-only OSS quick wins** (`oss-disclosure-template` + `fuzz-seed-lens`, Ranks 2-3). Both are S-effort, prompt-only, zero schema/test churn, and directly serve the active disclosure campaign — CWE+CVSS+references on every GHSA filing (`reporter.md` OSS branch currently has no CWE while the web/SC branches do), and higher fuzz yield on Bob's real codec/parser targets. Do these in the same pass as #1 while you're in the OSS code path.

3. **Build the patched-vs-unpatched differential oracle** (`differential-oracle`, Rank 4) as the strategic follow-through. Reuse the existing `bob_repo_docker_run` writable path (git checkout/apply inside the container), add the differential evidence row, gate to high/critical `oss_native_code`, keep it optional and x86_64-preferred — and **require a full (non-shallow) clone with `.git` present in `/src`** for the `upstream_fix`/`pre_introduction` controls (else they degrade to blocked; `self_patch` is unaffected). Combined with #1's grade-time gate, this turns Bob's residual-finding bread-and-butter into *defensible* HIGHs and pre-empts the "this was already fixed" pushback that recurs across Bob's disclosures.
