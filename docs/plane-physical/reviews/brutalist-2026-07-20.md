# Plane-PH third Brutalist adjudication

Review date: 2026-07-20  
Brutalist context: `866166da-6b7c-4029-80b7-3d371b7d8c1a`  
Mode: end-to-end architecture and implementation red-team  

## Evidence boundary

This was not a panel consensus. Codex produced the substantive review; the Agy
request failed at its prompt boundary and Claude failed during execution. The
surviving output is adversarial input, not repository evidence. Each disposition
below records the result of checking that input against the current code and
focused tests.

No Chameleon command, serial transaction, RF operation, credential, facility
system, or network target was exercised by this review or its remediations. A
topology-valid hypergraph is not a production verdict. All production gate
evidence and hardware-in-loop evidence remain independently required.

## Dispositions

| Finding | Disposition | Repository result |
| --- | --- | --- |
| The lifecycle custodian made the canonical core installer globally unavailable | fixed, bounded | Core JavaScript runtime packages now use a preflighted, staged, exact-root install path and can install, load, uninstall, and reinstall without a native-custodian preload. Optional native/provider mutations remain behind the fail-closed custodian. The core path is same-UID pathname custody, not a claim of hostile-principal or crash-atomic native custody. |
| Caller-created campaign closure could clear `CLAIM_FREEZE` | fixed, fail-closed | Campaign integrity and production assurance are separate. A structurally complete anchor remains non-satisfying until a server-issued obligation, physical-nucleus authority, enrolled production signer/witnesses, and durable no-active-effects attestation are all present. No current factory can mint those claims. |
| Test durable heads and callback ports could be consumed as production physical evidence | fixed, fail-closed | Experiment projections, Plane-PH gate evidence, and verdict resolvers now have explicit test/conformance trust classes. Production assertions reject every current test factory. There is deliberately no production issuer/backend factory yet. |
| The Chameleon failure matrix generated semantically false Cartesian cases and weak terminal classifications | fixed | Matrix v5 is built from a digest-pinned exact scenario/effect-to-operation relation, exact reverse bindings, and composite USB/BLE parity pairs. All 734 cases bind signed evidence contracts and require zero active effects plus the applicable restoration, containment, authority, residue, and witness predicates. References are obligations for a runner to materialize, never evidence by themselves. |
| Compiled provider bytes are not bound to the redeemed execution grant and worker fence | accepted, open blocker | The private compiler capability closes evaluator access to generic raw bytes, but the production path still needs one immutable object binding command/input digests to the redeemed grant, attempt, lease, resource generation, deadline, vault reservation, and worker-side fence recheck. |
| Parallel broker, launcher, stop/restore, and provider fixtures do not form one production composition root | accepted, open blocker | The repository has individually strict precursors but no single production root that owns grant redemption, worker launch, device transport, response custody, durable receipt/outbox publication, cancellation, restoration, and terminal evidence as one lineage. |
| Provider response bytes can cross the wrong custody boundary | accepted, open blocker | A production worker must commit raw responses directly to the independent vault/outbox and return only bounded summaries and opaque handles. Caller-shaped or agent-visible response bytes cannot be the evidence source. |
| Artifact/key custody could fork, omit archives, duplicate backups after lost acknowledgements, or retire keys before durable deletion evidence | accepted, remediation in progress | The vault/custody contract is being converged on durable stable seal intents, independently enumerated member custody, evidence reservation before external retirement, exact retry identity, plaintext zeroization, inherited-thenable rejection, and restore fencing. Production remains false until the external principal/key backend and hostile tests close. |
| A signed JavaScript worker closure can overstate its closed loader graph | accepted, release blocker | The current static loader analysis is being replaced/hardened after hostile proofs using computed `module.require`, optional `require`, `process.getBuiltinModule`, and loader aliasing. A signed byte set is not enough if the runtime can resolve an undeclared loader edge. |
| JavaScript worker release trust is mutable in-process and revocable material can be aliased | accepted, release blocker | Hostile tests proved mutable `crypto.createHash` can forge reported key identity and that the same Ed25519 SPKI can be reintroduced under another key ID. Intrinsic capture, canonical key-material uniqueness, and revocation/epoch high-water enforcement are required before the closure is trustworthy even as non-authorizing provenance. |
| Worker tests are green only because of an untracked sibling-package symlink | accepted, release blocker | The worker's private Chameleon dependency is absent from the root workspace/lock contract. Clean-checkout and packed-closure tests must construct the exact declared dependency graph without ambient `node_modules`. |
| Static Plane-PH checks can be mistaken for readiness | accepted, open blocker | `check-plane-physical` proves registry topology, pins, and invariants only. Node engineering gates, production-nonwaivable HIL gates, and `PH-X8` remain unclosed until their typed evidence objects are issued and verified. |
| Broad compiler/catalog coverage implies executable provider coverage | rejected as a completion claim | The semantic catalog and exhaustive source ownership are valuable design evidence, but only reviewed compilers joined to the production broker/worker lineage can make a capability executable. Unsupported hardware dimensions remain provider requirements rather than hidden coverage. |

## Dependency-correct production cut

The next useful vertical slice is deliberately narrow but end to end:

1. retain the now-working canonical core install;
2. close and reproducibly package the signed Node 20 worker dependency graph;
3. bind one reviewed RF-off HF14A probe compiler output to one redeemed grant,
   attempt, lease, resource fence, deadline, and pre-reserved vault sink;
4. execute it only through one worker-owned USB transport lineage;
5. commit raw response and exact receipt/outbox bytes outside the agent
   principal, returning only an opaque artifact handle and bounded summary;
6. publish through a production-qualified durable experiment head and exact
   campaign manifest; and
7. carry the verified transition through finding, grade, report, and
   SurfaceGraph composition before expanding the active compiler set.

This slice does not waive the rest of the provider surface. It establishes the
production authority/effect/evidence spine that every additional operation can
reuse.

## Verification recorded in this pass

- Chameleon failure-matrix contract: `5/5` with 734 exact cases.
- Physical campaign coordinator: `12/12` including an 18,207-event closure that
  cannot clear `PH-S12` without production attestation.
- Physical experiment contract: `22/22`; test projections cannot issue a
  production SurfaceGraph transition.
- Plane-PH gate-evidence contract: `16/16`; conformance evidence is rejected by
  the production assertion.
- Physical scope and capability consumers: `22/22`; raw callback verdict
  resolvers cannot be installed as production ports.
- Installer smoke suite after the direct core path: `24/24` on Node `20.19.4`.
- Static hypergraph checker: green at 48 nodes, 41 blocking hyperedges, 51
  coverage rows, 112 variants, 16 profiles, and 22 proof providers.

These are focused engineering results. They do not close the worker/vault
red-team findings, production composition root, broad release gates, or any
real-device HIL gate.
