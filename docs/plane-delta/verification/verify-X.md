I have everything I need. Here are the verdicts for my X-cluster nodes.

### X6 — Agent self-security
**VERDICT: keep** (anchor precision nit only).

1. **Already shipped?** No. `mcp/lib/envelope.js` is a name-collision red herring — it is the MCP `okEnvelope`/`errorEnvelope`/`ToolError` response wrapper (`mcp/lib/envelope.js:21-72`), not a datamark fence. Every "prompt injection / attacker-controlled" hit in the codebase is *offensive* bug-class knowledge for hunting injection in targets, not self-defense: `prompts/roles/evaluator.md:64` ("issues reachable from attacker-controlled network/file/API input") and `prompts/roles/orchestrator.md:60` ("taint trace from attacker-controlled input to dangerous sink"). There is zero nonce/datamark fencing of bytes entering Bob's *own* briefs. Genuine gap.
2. **Anchor real?** Yes, and accurate. The named untrust slices all exist in `mcp/lib/assignment-brief.js`: `traffic_summary` (:272), `audit_summary` (:273), `intel_hints` (:275), `schema_slice` (:277), `surface_graph_slice` (:278, :343) — these literally inline attacker-influenced summaries into the subagent prompt. `resolve-body.js` exists (`mcp/lib/tools/resolve-body.js`) and is the sanctioned full-body read of attacker-controlled artifacts (header comment: "Reads the FULL body of an artifact"); the actual body bytes come from `mcp/lib/body-resolvers/index.js` — the anchor should name that file too for the output-fencing point. `docs/LLM_AGENT_SECURITY_LANDSCAPE_2026.md` exists.
3. **Predecessors sound?** Yes. S13 (untrusted-envelope, net-new) is correctly unbuilt — no datamark primitive exists. No cycle.
4. **Anti-pattern check?** Clean. It's a hardening pass with a concrete consumer (every brief renderer + resolve-body). Not substrate-as-trajectory, not in `excluded_by_doctrine`.
5. **Effort/value sanity?** Δ2 plausible. Slice 1 (wrap 5 named brief slices + resolve-body output in a nonce envelope + one system preamble line) is genuinely small. The hidden cost is the fixture/regression suite proving "injection neutralized AND no regression in legitimate brief use" across every slice — non-trivial but tier-appropriate.

### X7 — Economic accountability
**VERDICT: split** (cheap avoided-run count vs. much-bigger dollar layer; predecessors over-coupled).

1. **Already shipped?** No. `mcp/lib/pipeline-analytics.js` has zero cost/savings/per-finding/dollar/token computation (grep empty across the 46KB file). Bob has no token telemetry at all (corroborated by memory `project_bob_oss_benchmark_plan`: "bob-oss has no MCP token telemetry"). Genuinely net-new.
2. **Anchor real?** Partially real, and harder than implied. `mcp/lib/pipeline-events.js` and `pipeline-analytics.js` exist, but `PIPELINE_EVENT_TYPES` is a **frozen closed enum** (`pipeline-events.js:28-52`, `Object.freeze([...])`) with no `evaluator_run_avoided`. Adding it requires: extend the frozen enum + the `compactEvent` field allowlist (`pipeline-analytics.js:371`) + satisfy `assertGovernanceContext` on every emit (`pipeline-events.js:127-143`, throws if `governance_context` absent) + a new analytics consumer. The node undersells this.
3. **Predecessors sound? — OVER-COUPLED.** Listed S11 + I11 are both unbuilt Δ4 net-new nodes. But a per-session `evaluator_run_avoided` **count** needs *neither* — it writes straight to the per-session `pipeline-events.jsonl`. Only the cross-session *aggregation* and *calibration-weighted* views need S11/I11. → **split**: X7-lite (per-session avoided-run + LLM-calls-avoided telemetry, no predecessors, could ride Δ2) vs X7-full (cross-session economic dashboard + calibration, needs S11+I11, Δ4).
4. **Anti-pattern check?** Clean *as written* — "DESCRIPTIVE ONLY, never an auto-kill gate" explicitly stays out of `excluded_by_doctrine` X-rej-1/X-rej-2 (the auto-kill/prefilter inversions). Keep that guard in the detail spec; it is the only thing keeping this node legal.
5. **Effort/value sanity?** The "cost-per-finding" (dollar) sub-claim is **much bigger than the Δ4 tag implies** — it presupposes a transcript/token-ingestion substrate Bob does not have (the benchmark-plan memory notes cost is only reconstructable from Claude Code transcripts, not MCP state). "evaluator_run_avoided count" is cheap; "$ cost-per-finding" is a separate substrate project. The split above resolves the mis-sizing.

### X8 — Live in-run observability
**VERDICT: keep** (acknowledge an existing weak poll-based live view).

1. **Already shipped?** Partially — and the node should say so. `mcp/lib/dashboard.js` already serves a live-ish view: the HTML client polls `/api/snapshot` with `cache:"no-store"` (`dashboard.js:482`). So a crude auto-refresh "live view" exists today. What's absent is push-based SSE, mid-run re-attach, and merged verification/wave ledgers. The node's value-add is real but it's an *upgrade*, not a from-scratch capability.
2. **Anchor real?** Yes, exact. `routeDashboardRequest` exists (`dashboard.js:545`) and currently routes only `/` and `/api/snapshot` (`:552`, `:556`); the new `/api/session/:domain/events` SSE route slots cleanly into it. `http.createServer(...routeDashboardRequest...)` at `:582`, loopback-only warning at `:574-580` (matches the "loopback-only" gate). The stream source `mcp/lib/frontier-events.js` + `frontierEventsJsonlPath` exists.
3. **Predecessors sound?** Yes. "(dashboard.js)" is accurate and self-contained. No cycle.
4. **Anti-pattern check?** Clean — and notably *aligned* with the doctrine. `excluded_by_doctrine` X-rej-4 rejects "in-memory event bus" because "frontier-events.jsonl already IS the event stream"; X8 proposes a **file-backed** SSE tail *over that same jsonl*, i.e. it obeys the rejection's reasoning rather than violating it. Good.
5. **Effort/value sanity?** "Δ2 (polish)" is honest. One SSE route on an existing loopback server + a tail-with-Last-Event-ID reader. Small. Lowest-stakes node in my cluster.

### X9 — Orphaned-consumer CI
**VERDICT: revise → fold into the existing stigmergy-coherence gate; anchor is STALE.**

1. **Already shipped? — YES, the mechanism is.** X9's exact core ("flag fields CONSUMED in briefs/prompts with NO producer") is already implemented as `checkAssertionC` in `scripts/check-stigmergy-coherence.js:131-144` (`consumer_references_unknown_producer`). The inverse (inert producer) is `checkAssertionA:67-90` (`producer_has_no_consumers`/`producer_consumers_not_manifested`), and "the consumed token actually resolves at the cited source file" is `checkAssertionB:92-129` (`consumer_token_unresolved`). It is backed by manifests `mcp/lib/stigmergic-producers.js` + `mcp/lib/stigmergic-consumers.js` and **wired into CI**: `check:stigmergy-coherence` → `test:prompts` → `npm test` (`package.json:72,74,82`).
2. **Anchor real? — STALE.** The named anchor `scripts/check-orphaned-consumers.js` does not exist and *should not* be created — it reinvents `scripts/check-stigmergy-coherence.js`. The real anchor is that existing script + the two manifests.
3. **Predecessors sound? — partly wrong.** Listed S12 + X4. X4 is real (`test/determinism-ci.test.js` exists), but the *truthful* predecessor X9 fails to cite is the Y.9 stigmergy-coherence system. Worse, X9 **overlaps S12** itself: both are "a manifest catches the silent drop" — S12 over `test/mcp-test-manifest.json`, X9 over consumer fields. Consider merging S12+X9 into one manifest-integrity story.
4. **Anti-pattern check?** Clean doctrinally, but it trips the graph's *own* "index/check without consumer" spirit by duplicating a shipped check.
5. **Effort/value sanity? — mis-framed.** The genuine net-new is NOT a new script; it's **coverage**: the reachability drop (finding #1) slipped because that producer/consumer pair was never *registered* in `stigmergic-consumers.js` (only 6 consumers manifested today: `stigmergic-consumers.js:37,48,59,70,81,92`). So the real Δ-work is (a) register the brief/prompt-consumed fields (reachability etc.) in the existing manifest so `checkAssertionC` catches future drops, and optionally (b) add an *auto-extractor* that populates the consumer manifest from brief/prompt field reads instead of hand-curation. That is a smaller, sharper task than "new orphaned-consumer CI."

### X-cluster summary
- **X9 is the headline correction: demote/revise.** Its central capability is already shipped and CI-wired as `check-stigmergy-coherence.js` `checkAssertionC` (consumer→missing-producer) + `checkAssertionA` (inert producer) + `checkAssertionB` (token-resolves-at-source), backed by `stigmergic-producers.js`/`stigmergic-consumers.js` and run in `npm test`. The proposed anchor `scripts/check-orphaned-consumers.js` is stale and would duplicate it. The reachability drop escaped only because that field-pair was never *registered* in the manifest — so the real work is manifest coverage/auto-population, and X9 should fold into the existing gate (and arguably merge with S12, which is the same "manifest catches the drop" pattern).
- **X7 needs a split and an honesty edit.** Per-session `evaluator_run_avoided` counting is cheap and needs neither S11 nor I11; the "$ cost-per-finding" layer is a separate, much-bigger substrate (Bob has no token telemetry — only Claude Code transcripts carry it) and is the only part that justifies Δ4 + the S11/I11 predecessors. Also note `PIPELINE_EVENT_TYPES` is a frozen, governance-gated enum (`pipeline-events.js:28`), so even the cheap part is more than a one-line event add.
- **X6 keep, anchor nit:** real gap (no datamark self-defense anywhere; `envelope.js` is an unrelated response wrapper); slices verified at `assignment-brief.js:272-278`; add `mcp/lib/body-resolvers/index.js` to the anchor alongside `resolve-body.js` for the output-fencing path.
- **X8 keep, scope honesty:** the dashboard already polls `/api/snapshot` (`dashboard.js:482`) for a weak live view, so X8 is an SSE *upgrade*, not greenfield; its file-backed-tail design correctly respects `excluded_by_doctrine` X-rej-4. Smallest/safest node.
- **No X-node drifts into `excluded_by_doctrine`** — none introduce auto-kill, pre-verify prefilter, hunt-brief exemplars, RAG, or an in-memory bus. X7's "descriptive-only, never auto-kill" guard is load-bearing and must survive into the detail spec.