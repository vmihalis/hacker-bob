### S11 — Cross-session memory store
**VERDICT: revise (lean demote-partial) — the cross-session READ substrate + sanitizer + authority class already exist; the I6 justification is stale.**

- **Already shipped (partial).** A sanctioned cross-session aggregation substrate is live: `mcp/lib/pipeline-analytics.js:1152` emits `mode: "cross_session"` and walks every session dir (`candidates.domains`, `CROSS_SESSION_ANALYTICS_MAX_SESSIONS`). There is a dedicated authority class `cross_session_read` (`mcp/lib/session-authority.js:32,236,350,883`; `mcp/lib/tool-telemetry.js:55` source value `"cross_session"`) whose contract is literally "bounded/sanitized cross-session telemetry" (`scripts/authority-inventory.js:290`, `:410`, `:607-608`). The PII/secret sanitizer the node's gate calls for already exists: `mcp/lib/sensitive-material.js:73` `validateNoSensitiveMaterial`, `:118` `redactTextSensitiveValues`. So "sanctioned cross-session label/metric, no raw PII/secrets" is ~70% built under another name.
- **Stale anchor justification.** "the parked I6 cross-session prereq" is wrong: I6 (findings vector index) is shipped and *already* injects cross-target (= cross-session) priors into briefs — `docs/capability-hypergraph.md:196-204` (I6 "top-K cross-target priors") and `:360` (C6). I6 needs no new store as a prereq.
- **Anchor real?** `mcp/lib/cross-session-store.js` correctly marked new (does not exist, confirmed). But the proposed global `~/hacker-bob-sessions/.cross/` JSONL drifts from the established per-session-JSONL + `cross_session` reader pattern and would sidestep the `cross_session_read` authority gate — a mild index-without-the-existing-consumer / parallel-substrate smell.
- **Predecessors/tier.** S1,S2 sound; Δ4 placement (lowest priority) fine. The genuinely-missing slice is only a durable label/metric *write* target for I11/X7 — small, and should reuse the existing reader + authority + `sensitive-material` rather than a new global dir.

### S12 — Test-manifest integrity gate
**VERDICT: revise — substantially pre-shipped; anchor mislocated and omits the file that already does 80% of the job.**

- **Already shipped (partial).** `test/mcp-test-discovery.test.js` already enforces most of the intent: line 28 fails CI if a manifest entry's file is missing (`${file} does not exist`), line 26 rejects duplicates, and line 31 (`assert.deepEqual(manifestMcpTests, discoveredMcpTests())`) forces every on-disk test to be registered. It runs in CI via `package.json:73` `test:mcp` → `scripts/run-mcp-tests.js`.
- **Real gap is narrow.** Line 17's regex `^mcp-.*\.test\.js$` means the discovery-sync only covers `mcp-`-prefixed tests. Non-prefixed tests — including exactly the ones I9 wants to re-arm, `test/repo-target.test.js` + `test/dashboard.test.js` — are NOT discovery-checked, and there is no lib-module→guard-test mapping. So the existing check would *not* have caught the reachability-producer drop. S12's root-cause claim is therefore only half-valid: the systemic hole exists, but the fix is "extend the existing partial check," not "new substrate."
- **Stale/mislocated anchor.** The node cites `scripts/run-mcp-tests.js` (which does NO integrity work — only array-of-strings validation, `:23-25`) plus a NEW `scripts/check-manifest-integrity.js`, and never names `test/mcp-test-discovery.test.js`, the file that already implements the core. Re-anchor to extend that test (drop the mcp- prefix filter + add module→test coverage).
- **Predecessor/tier.** Predecessor S5 (tool registry) is loosely related at best — this gate is about test files. Δ1 (cheap) plausible. Downgrade from "harden net-new" framing to "broaden existing `mcp-test-discovery` assertion."

### S13 — Untrusted-content envelope
**VERDICT: keep (minor predecessor revision).**

- **Not shipped — genuinely new.** No datamarking/fencing exists anywhere (grep for datamark/untrusted/fence/spotlight returned only unrelated `nonce`/`geofence`/`checkpoint` hits). Note the namespace trap: `mcp/lib/envelope.js` is the unrelated ToolError *error*-envelope (`:21 class ToolError`), not a content envelope — do not let reviewers think this is already covered.
- **Anchor real?** `mcp/lib/untrusted-envelope.js` correctly new (does not exist). The `crypto.randomBytes` nonce model is real and citable: `mcp/lib/session-cap.js` exists (confirmed). The X6 consumer surface is real: `mcp/lib/tools/resolve-body.js` + `mcp/lib/body-resolvers/` (8 resolvers incl. `http-record.js`, `frontier-event.js`, `evidence-pack.js`) exist.
- **Predecessor flag.** S5 (registry, for a tool-output-boundary apply) is plausible, but **S7 is mis-attributed** — S7 is HTTP-scan auth injection ("credentials never enter LLM context"), only tangential to fencing attacker bytes. The real missing predecessor is the brief/body-resolver substrate (S8 routing / S10 evaluator brief), since the envelope wraps bytes as they enter the brief. Swap S7 → S10.
- **Anti-pattern/tier.** Clean: substrate with a same-tier consumer (X6, Δ2); not in `excluded_by_doctrine`. Δ2 slightly optimistic — the primitive (S13) is tiny; the *wiring* (X6 across all untrust slices + body-resolver + system preamble) is the larger half, correctly parked in X6.

### S14 — Differential control-build substrate
**VERDICT: revise — the writable-rebuild half is real, but the clone anchor directly contradicts Bob's Plane-O policy.**

- **Already shipped (half).** "Reuse `bob_repo_docker_run` writable path" is real: `mcp/lib/repo-env.js:763` `REPO_MOUNT_MODE_VALUES = ["read_only","read_write"]`, plus a writable `/work` staging area (`:856 -v ${workDir}:/work:rw`) and an explicit read-only-mount→writable-staging pattern (`:316-385`). So in-container rebuild is feasible today.
- **Stale/contradictory anchor — the load-bearing defect.** "bob_init_repo_session full-clone flag" violates doctrine: `mcp/lib/tools/init-repo-session.js:35` "Plane O O-P1 forbids remote clones; the path must already exist as a directory," `:38` "Stored for provenance; never used to clone," and `mcp/lib/repo-target.js:11` "No clone-from-remote happens here." Bob **cannot clone** — the operator brings a local checkout, so there is no clone to add a flag to. Worse, the container defaults to `--network none` (`mcp/lib/repo-env.js:838`), so in-container `git fetch` of a control ref is blocked too. The control ref must therefore already exist in the operator's local-checkout history.
- **No git-checkout codepath exists.** Bob only validates commit-hash *format* (`mcp/lib/repo-target.js:110`); it never runs `git checkout`. So S14 needs a net-new in-container `git checkout <fix_ref>` against the operator's history (default `/src` is read-only, `repo-env.js:850-855` — needs `read_write` mount or `/work` staging) AND git present in the toolchain image. That is more than "adopt/reuse."
- **Predecessor/tier.** S6 (static artifact import) is a weak predecessor — the true predecessor is the OSS repo-session/docker-run substrate, which is unnumbered in the slab register (gap). Δ2 / "adopt" understates effort given the no-clone/network-none/read-only-default constraints. Revise to: "differential control-checkout (operator-history, no-clone)" — replace the clone-flag anchor with (a) a shallow/`--depth 1` detection + control-ref-reachable guard on the bound checkout, and (b) a `read_write`/staged in-container `git checkout`; bump the effort estimate.

### S-cluster summary (most important corrections)
1. **S14 anchor is wrong on policy, not just naming.** "bob_init_repo_session full-clone flag" is forbidden by Plane-O O-P1 (`init-repo-session.js:35,38`; `repo-target.js:11`) and undermined by `--network none` (`repo-env.js:838`). Keep the node — differential control proof is valuable and the `read_write`/`/work` substrate exists (`repo-env.js:763,856`) — but reframe as an operator-history shallow-clone *guard* + a net-new in-container `git checkout` (currently absent), and stop claiming "reuse" — it's bigger than Δ2/adopt implies.
2. **S12 is ~80% already implemented** in `test/mcp-test-discovery.test.js` (entry-exists check line 28, dup check line 26, discovery-sync line 31) — but only for `mcp-`-prefixed tests, so it would NOT have caught the reachability drop. Re-anchor to *extend* that test (drop the prefix filter, add module→test coverage); the node's cited anchors (`run-mcp-tests.js`, a new script) miss the file doing the real work.
3. **S11 partially duplicates an existing cross-session substrate** — `pipeline-analytics.js:1152 mode:"cross_session"`, the `cross_session_read` authority class (`session-authority.js:32`, `authority-inventory.js:290`), and `sensitive-material.js` sanitizers. Its "parked I6 prereq" rationale is stale (I6 cross-target priors already ship, `capability-hypergraph.md:196-204,360`). Re-scope to a thin label/metric writer over the existing reader+authority+sanitizer; drop the new global `~/.cross` dir.
4. **S13 is the cleanest keep** — genuinely new (no datamarking exists; `envelope.js` is the unrelated ToolError envelope), real consumer (`resolve-body.js`), real nonce model (`session-cap.js`). Only fix: predecessor S7 is mis-attributed; the brief substrate (S10) is the real prerequisite. None of S11–S14 drift into `excluded_by_doctrine` (no auto-kill, no FP-prefilter, no RAG).