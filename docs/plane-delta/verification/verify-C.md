### C9 — OSS severity-ceiling closure
**VERDICT: revise** (split off an already-shipped sub-claim; keep the grade-time gate).
- **Already shipped?** The grade-time reachability gate is NOT shipped — `mcp/lib/lifecycle-gates.js` and `mcp/lib/grade-verdict-store.js` contain zero `reachab*/attack_vector/ceiling/AV:` refs (grep empty). The only existing severity gate is the floor in `grade-verdict-store.js:228` (`requireFinalReportableSeveritySet` → `.filter(r => r.reportable && isMediumOrHigher(r.severity))`), which is a reportability floor, not a reachability lift/ceiling. **BUT** the node's stated mechanism also names "chain severity elevation" — that is ALREADY shipped: `prompts/roles/chain.md:8-10,49` already encodes the ladder (LOW+LOW ≤ LOW; MEDIUM+MEDIUM ≤ MEDIUM unless a `severity-elevation rationale:` line). C9 must DROP the chain-elevation portion (DEMOTE→already-covered) and keep only the grade-time, reachability-driven lift/ceiling.
- **Anchor real?** Yes. Both files exist; the new gate slots above/beside `requireFinalReportableSeveritySet` (`grade-verdict-store.js:191-231`). Correctly located.
- **Predecessors sound?** Yes — sole predecessor I9. **I9's "dropped producer" premise is CONFIRMED true**: `repo-target.js:752 buildRepoInventory` no longer emits any reachability fields (grep inside the file matches only the signature + export); the only `max_credible_severity_ceiling/native_attack_vector_map/network_reachable_anchors` reference left in `mcp/` is the *consumer* `dashboard.js:186-189`; and `test/repo-target.test.js` is orphaned (NOT in `test/mcp-test-manifest.json`, and fails to even load — `require("../mcp/lib/hunter-completion.js")` MODULE_NOT_FOUND). So C9's predecessor is genuinely needed and well-founded.
- **Anti-pattern check** Clean. The gate runs at GRADE time on the final verification round (post-freeze, post-verify) — it does NOT invert freeze-then-verify (X-rej-2 targets a *pre-verifier* prefilter). Caution: "suppress AV:L inflation" must CAP severity, never drop the finding from the reportable set — a cap is fine, a kill would drift toward no-premature-termination violation.
- **Effort/value** Δ1 plausible and high value-to-effort; the gate itself is small, value rides entirely on the I9 restore (also Δ1).

### C10 — Patched-vs-unpatched differential proof
**VERDICT: keep.**
- **Already shipped?** No. `mcp/lib/evidence.js` + `mcp/lib/tools/write-evidence-packs.js` have zero `control/differential/vuln_run/control_run/upstream_fix/pre_introduction/self_patch` refs (grep empty). The pack shape today is data-exposure-style (`evidence.js:80-119`: `aggregate_counts/representative_samples/sensitive_clusters`). The `control_kind` enum (`upstream_fix/self_patch/pre_introduction`) is net-new repo-wide.
- **Anchor real?** Yes. Both files exist (`EVIDENCE_PACKS_VERSION = 1`, `evidence.js:42`); adding a `{vuln_run_id, control_run_id, control_kind, verdict}` row is a clean extension of the existing validated pack writer.
- **Predecessors sound?** Yes — S14 (control-build) reuses the real `bob_repo_docker_run` writable path + a non-shallow-clone flag on `bob_init_repo_session`. Both tools exist. No cycle.
- **Anti-pattern check** Clean — differential proof is the doctrine-aligned way to rebut "already fixed" (the residual-hunting the rules explicitly demand).
- **Effort/value** Δ2 OK. Note the real engineering weight is in S14 (in-container checkout+rebuild of a control commit); C10's own evidence-row/verdict surface is modest.

### C11 — Static-analysis-driven source audit
**VERDICT: keep** (with a scope correction on predecessors — consumer plumbing partly exists, source does not).
- **Already shipped?** Consumer-side plumbing PARTLY exists: `lead-promotion.js:60` already references `bob_static_scan`, and `:66/:242` already implement `isExternalProducerLead` + `enforceLeadRationalePolicy` (an external-producer-lead rationale gate). `cli-tool-packs.js:220-222` already seeds `semgrep --config auto /src` + trivy, and `assignment-brief.js:384` lists them. **However** `bob_static_scan` is a token-contract-only web3 scan (`tools/static-scan.js` → `staticScan`, `scan_type` enum is just `token_contract`), NOT a general SARIF/CodeQL source-audit. So the SARIF→lead path (I10/IP7) is genuinely net-new; the consumer (`lead-promotion`) and the semgrep runner (IP7-partial) already exist.
- **Anchor real?** Yes — `lead-promotion.js` exists; OSS lens lives in `evaluator.md:64` (`oss_native_code` source/sink guidance). Correct sites.
- **Predecessors sound?** Yes, all three real-but-unbuilt; no cycle. I9 = dropped/restorable (confirmed above). **I12 confirmed wired-but-never-produced**: `assignment-brief.js:403` consumes `context.ossTechniquePacks` in a `briefSliceEntry`, but a producer-side grep for any assignment to `ossTechniquePacks` is EMPTY → inert consumer, exactly the orphaned-consumer class. I10 = net-new (`static-analysis-index.js` absent). C11 is a 3-way fan-in and cannot ship until all three land — heaviest dependency weight in the cluster.
- **Anti-pattern check** Clean — I10/I12 feed LEADS not auto-findings, and `lead-promotion`'s existing rationale policy is a real guardrail against hallucinated-correlation-as-feature.
- **Effort/value** Δ3 OK; flag the predecessor depth (I9 restore + I10 + IP7 + I12 all gate it).

### C12 — Coverage-guided fuzzing maturity
**VERDICT: revise** (the prompt-lens half is largely already shipped; anchor line is mislabeled; effort under-tiered).
- **Already shipped?** The fuzz LENS is largely PRESENT in `evaluator.md`: `:57` `fuzz_run` task lens (bounded fuzz/ASAN/sanitizer in docker), `:59` corpus staging via the `compose` role, `:64` "sanitizer/fuzzer-repro candidates" + "minimal build/test/fuzz/sanitizer command", `:81` "fuzzer crashed" blocked_harness. So the "evaluator.md fuzz lens" anchor is mostly redundant. What is genuinely missing: `recommendedCommandsFor` emits NO fuzz command today (grep for a `role:"fuzz"` emission is empty), plus plateau detection / structure-aware harness / sanitizer matrix / arch-portability.
- **Anchor real? STALE LINE.** The node points at `repo-env.js:141` to "emit fuzz role" — but `:141` is the literal `"fuzz",` entry inside the frozen `RECOMMENDED_COMMAND_ROLES` enum (`repo-env.js:138-144`); the role is already a valid enum value. The actual emission site is `recommendedCommandsFor` at `repo-env.js:207` (which currently emits only build/test/compose). Fix the anchor to `:207`.
- **Predecessors sound?** Yes — IP8 (fuzz-seed corpus).
- **Anti-pattern check** Clean.
- **Effort/value** **"adopt" under-tiers this.** Structure-aware harness synthesis + plateau detection + sanitizer matrix + aarch64 compiler-rt portability is substantial net-new engineering — the libde265 / libcupsfilters / gpac-aarch64-ASAN memories all document this as a hard surface. Treat as net-new, Δ3-heavy, not a lens adopt.

### C13 — Cross-target finding transfer
**VERDICT: revise** (anchor under-specified; cross-target dispatch and scope-authorization are load-bearing and missing from the anchor).
- **Already shipped?** No. The wave system is single-domain: `tools/start-wave.js:26` requires `target_domain`; `cross_target` appears only in I6 similarity queries (`claim-clusters.js:109-112`) and metric scoping (`read-capability-metrics.js:14`), never in wave dispatch.
- **Anchor real? Incomplete.** "new orchestrator routine + `bob_start_wave` dispatch" — `bob_start_wave` exists but only starts a wave on an already-initialized single target. Transferring to target B requires `bob_init_session(targetB)` FIRST (`tools/init-session.js:21`, `target_domain` required) PLUS a scope/authorization gate proving B is in an authorized program. The anchor names only `bob_start_wave` and omits both.
- **Predecessors sound?** Yes — I13 (net-new) fans in over three SHIPPED indexes (`symbol-surface-index.js`, `findings-index.js`, `surface-graph.js`), which is exactly the cheap-because-they-exist fan-in the topology claims. No cycle.
- **Anti-pattern check** Scope-safety is the live risk: transfer must dispatch ONLY to authorized targets. The gate text ("2nd authorized target") is right, but the enforcement must live in the dispatch routine (a hard scope check), not just the prompt — call this out explicitly.
- **Effort/value** Δ4 frontier OK, but effort is bigger than the anchor implies (per-target session init + scope gate + I13).

### C14 — Proof-carrying disclosure
**VERDICT: split** (metadata half is adopt-extend of an existing pattern + already a Δ1 ride-along; bundle half is the net-new Δ2 work).
- **Already shipped?** Partially. CWE machinery is ALREADY precedented in `reporter.md` — but on the SMART-CONTRACT branch: `:38` a `CWE` field and `:49-57` a full canonical family→CWE mapping table. The OSS branch (`reporter.md:28-34`) has reachability/impact/FP/remediation but NO CWE/CVSS/refs. So C14's "CWE + CVSS-4.0 + refs on the OSS branch" is adopt-and-extend of the existing SC CWE pattern, NOT pure net-new. The genuinely net-new piece is the machine-checkable artifact bundle (replay script / invariant / differential control).
- **Anchor real?** Yes, with a path nit. `reporter.md:28-31` is the OSS branch region (correct insertion site). `SECTION_KINDS` is REAL at `compose-report.js:49` — but the file is `mcp/lib/tools/compose-report.js` (node omits `tools/`); the symbol and line are correct (`SECTION_KINDS` frozen array at :49, consumed at :259/:432, exported :473).
- **Predecessors sound?** Yes — C10 (supplies the differential-control artifact) + I12 (family→CWE). Both real-but-unbuilt; no cycle.
- **Anti-pattern check** Clean.
- **Effort/value** The topology ALREADY implies the split (it schedules "C14-lite" as a Δ1 ride-along with no C10 dep, and "C14-full" at Δ2). Make it explicit in the node: C14-lite = CWE/CVSS/refs prose (type adopt, Δ1, no C10) vs C14-full = bundle attach (net-new, Δ2, needs C10). The node's flat "net-new / Δ2" mislabels the cheap half.

### C15 — Adversarial self-patch loop
**VERDICT: keep** (largest-effort node; one doctrine guardrail to state).
- **Already shipped?** No — zero `self_patch/self-patch/adversarial.*patch` refs anywhere in `mcp/` or `prompts/`.
- **Anchor real?** Yes — the `chain-builder` agent exists (`.claude/agents/chain-builder.md:2 name: chain-builder`, with TaskGraph read-write authority per `:85`). "new orchestrator routine" is net-new. Correct.
- **Predecessors sound?** Yes — C10 (the `self_patch` `control_kind` is the differential primitive C15 generalizes) + S14 (apply + rebuild the candidate patch in-container). Clean dependency, no cycle.
- **Anti-pattern check** Clean as framed (a post-finding hardening loop, doesn't touch freeze-then-verify). ONE caution to encode: a candidate patch that "fixes" the PoC must NOT auto-suppress the original finding — the pre-patch vuln is still real. Keep the loop additive ("stronger finding + suggested fix"), never a kill path.
- **Effort/value** Δ4 correct (lowest priority, gated behind C10+S14). But "net-new orchestrator routine + chain-builder integration" undersells the largest node in the cluster — a full propose→rebuild→re-attack→iterate-N loop. Flag effort as high; keep it last.

### C-cluster summary (most important corrections)
1. **C9 double-counts an already-shipped capability.** "Chain severity elevation" already exists in `chain.md:8-10,49`. C9's genuine net-new is ONLY the grade-time reachability lift/ceiling (no such gate in `grade-verdict-store.js`/`lifecycle-gates.js`). Trim the intent. The grade-time gate is doctrine-clean (post-verification) provided the AV:L ceiling caps severity rather than dropping the finding.
2. **The whole Δ1 premise checks out** — I9's "silently dropped producer" is real: `repo-target.js:752` no longer emits reachability, only `dashboard.js:186-189` consumes it, and `test/repo-target.test.js` is orphaned (not in the manifest, won't even load — broken `hunter-completion.js` require). C9 is well-founded on it. Likewise I12 (C11's predecessor) is confirmed wired-but-never-produced (`assignment-brief.js:403` consumes `context.ossTechniquePacks`; no producer exists).
3. **C12 is mis-anchored and under-tiered.** Anchor `repo-env.js:141` points at the role ENUM, not the producer (`recommendedCommandsFor:207`); the evaluator.md fuzz lens already exists (`:57/:59/:64/:81`). The real, heavy work (structure-aware harness, plateau detection, sanitizer/aarch64 portability — per the libde265/libcupsfilters/gpac memories) makes "adopt" wrong; this is net-new Δ3-heavy.
4. **C13's anchor is incomplete and scope-risky.** `bob_start_wave` is single-domain; cross-target transfer additionally needs `bob_init_session(targetB)` and a hard scope-authorization gate enforced in the dispatch routine (not just the prompt). State both.
5. **C14 should be an explicit split.** CWE/CVSS/refs is adopt-extend of the existing smart-contract CWE table (`reporter.md:49-57`) and is already a Δ1 ride-along ("C14-lite"); only the machine-checkable bundle is the net-new Δ2 work needing C10. The flat "net-new/Δ2" label is wrong for the cheap half. Path nit: anchor is `mcp/lib/tools/compose-report.js:49` (omit-`tools/` is the only error; `SECTION_KINDS` at :49 is correct).
6. **No C-cluster node drifts into `excluded_by_doctrine`.** Two guardrails to encode explicitly: C9's ceiling must cap-not-kill, and C15's self-patch loop must never auto-suppress the pre-patch finding. C13/C15 are correctly the largest-effort, lowest-priority Δ4 nodes (gated behind I13, and C10+S14 respectively).