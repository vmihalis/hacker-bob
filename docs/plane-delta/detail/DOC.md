# DOC — Correct stale shipped-claims in capability-hypergraph.md
**Kind / Tier / Type:** DOC · Δ1 · harden   **Verdict from verification:** new (doc-fix node split from WEAVE.md Must-fix #1 into `doc_fixes[]` DOC-1 + DOC-2; both code-verified this session).

## Spec (one-paragraph contract)
**Input:** the shipped roadmap `docs/capability-hypergraph.md` plus two code-verified ground-truth facts — (a) `mcp/lib/findings-index.js` and its tool wrappers were deleted in commit `c02179c` "Delete finding-store, findings-index, findings: claims is solo" (ejmockler, 2026-05-28), and (b) the OSS reachability/severity-ceiling *producer* was dropped at the Plane-O (v2.1) merge, leaving inert consumers. **Output:** an edited markdown doc whose I6 section and progress-log no longer assert a deleted index as live, plus one new progress-log entry recording the I9 producer drop. **Predecessors:** none (root doc-truth fix). **Context budget:** none — this is a direct `Edit`/`Write` on a `docs/` file, no MCP brief or subagent is introduced; it does not touch any MCP-owned or audit-graded artifact.

## What it does / holds
It removes a doc-vs-code drift so the roadmap stops lying about what HEAD contains. Two concrete corrections:

- **DOC-1 (I6 "all shipped" is a phantom).** `docs/capability-hypergraph.md:194-206` ("### I6 — Findings vector index") still says *"Engineering-complete pending parishioner gate. Library, MCP tool wrappers (`bob_index_candidate_claim`, `bob_query_candidate_claims_index`), auto-indexing in `bob_record_candidate_claim`, and brief-time prior injection (`priors_slice` …) all shipped."* Verified at HEAD: `mcp/lib/findings-index.js` does not exist; `grep priors_slice|summarizePriorFindingsForSurface|findings-index mcp/lib/assignment-brief.js` → empty; neither `bob_index_candidate_claim` nor `query_candidate_claims_index` exists in `mcp/lib/`; `calibration_label` is gone from `mcp/lib/`. The three progress-log entries at `:570-572` likewise narrate `mcp/lib/findings-index.js` as a shipped slice. The only survivor of the cross-target-prior idea is `mcp/lib/claim-clusters.js:115 mergePriorClaimMatches` (exported `:146`), and it is an **orphaned producer** — consumed only by `test/claim-clusters-priors-saturation.test.js`, with zero production caller.
- **DOC-2 (record the I9 producer drop).** Add a `## Progress log` entry (newest-first, per the header at `:549`) noting that the OSS reachability/severity-ceiling producer was silently dropped: no `reachability` key is produced anywhere in `mcp/lib/repo-target.js` (`buildRepoInventory` at `:751`), while consumers remain wired and inert — `mcp/lib/dashboard.js:186-189` reads `inventory.reachability.{max_credible_severity_ceiling,network_reachable_surface_ids}`, `mcp/lib/assignment-brief.js:140-141` whitelists `severity_ceiling`/`network_reachable` scalar caps, and `mcp/lib/wave-brief-derivation.js:133-134` (+ comment `:174-178`) caps the same fields. The recovery blobs are `d1647c0` ("Add OSS reachability/severity-ceiling triage and residual-fix hunting") and `1a456f1` ("Add per-path reachability attribution to the OSS native surface (#7)") — both confirmed to exist via `git cat-file`.

## Failure mode it prevents
A future maintainer (or a Bob orchestrator reading its own roadmap) treats I6's cross-target prior retrieval and the OSS reachability triage as *live capabilities* and either (a) builds on top of a deleted module — re-introducing `findings-index.js` imports that fail at runtime — or (b) ships I13/C13 (cross-target transfer) against the assumption that "retrieval-into-brief already works," when in fact the only surviving primitive (`mergePriorClaimMatches`) is unconsumed. For DOC-2: a reader assumes OSS findings already get a defensible severity ceiling, never noticing that C9's grade-time lift has no producer to read from. The doc is the single source of "what's done" in this repo; a stale "realized" claim is the most expensive kind of drift.

## Predecessors & unlocks
- **Predecessors:** none. This is a leaf correction; it depends on no other Δ node.
- **Unlocks:** `doc-truth` — the precondition for honestly detailing I9 (restore), I11 (calibration labels must source from grade/adjudication, *not* the deleted `calibration_label` slot), I13/C13 (repoint predecessor from `findings-index` to the claims subsystem). **Cross-tier note:** DOC is Δ1 but its correctness gates the *framing* of Δ3/Δ4 nodes (I13, C13) — those nodes' specs cite the claims subsystem precisely because this fix establishes that `findings-index` is gone. No build dependency, but a semantic one.

## DO (build slices)
Each slice is a self-contained `Edit` on `docs/capability-hypergraph.md`; ship as one commit (doc-only).

1. **(DOC-1, primary anchor) Rewrite the I6 section status, `:194-206`.** Replace the "all shipped" `**Status.**` paragraph with a `**Status. SUPERSEDED — index deleted.**` note: state that `mcp/lib/findings-index.js`, `finding-store.js`, `findings.js`, and the `bob_index_candidate_claim` / `bob_query_candidate_claims_index` tools were removed in `c02179c` ("claims is solo"); that no `priors_slice` injection survives in `assignment-brief.js`; and that the only residual cross-target-prior primitive is the orphaned `mergePriorClaimMatches` in `mcp/lib/claim-clusters.js:115`. Keep the original `**What it holds / Failure mode / DO**` prose but prefix the subsection with a "(historical design — not in HEAD)" marker so the design intent survives for the I13 re-port without reading as current state.
2. **(DOC-1) Annotate the three stale progress-log entries at `:570-572`.** These are append-only history, so do **not** delete them; append a bracketed retraction to each — e.g. `[RETRACTED c02179c: findings-index.js deleted; see DOC-1 / 2026-06-07 entry]` — preserving the log's append-only contract stated at `:549`.
3. **(DOC-1, secondary references — flag and fix) Correct the other live-I6 assertions.** Grep confirms five more stale spots the narrow anchor misses: `:360` (C6 status: "priors_slice … which it has"), `:363` (pedagogical note "`I6` is the retrieval layer"), `:410` (X3 lists "I6 candidate-claims index"), `:424` (X5 fixture "I6 ranks an IDOR claim ahead of an XSS"), and the `## Completion summary` at `:11,13` (the "102 MCP tools / engineering task graph is realized" count is stale — `c02179c` removed `tools/index-candidate-claim.js` + `tools/query-candidate-claims-index.js`). Fix `:360`/`:363`/`:410`/`:424` to past-tense/superseded and add a one-line caveat to the completion summary that the post-v2 "realized" status was partially walked back by `c02179c` (I6) and the v2.1 merge (I9). *(Scope call: the node's verified anchor is `:194-206,570-572`; slices 1-2 close the named drift, slice 3 is the "don't leave it half-done" completion. If kept minimal, slice 3 must at least leave a forward-pointer so the secondary mentions aren't silently contradictory.)*
4. **(DOC-2) Prepend the I9 producer-drop entry to `## Progress log`.** Insert at the top (newest-first, after the `:549` header line) following the established `- **DATE** · item · slice · detail · status` format: `- **2026-06-07** · I9 (regression record) · OSS reachability/severity-ceiling PRODUCER dropped at the v2.1/Plane-O merge · No `reachability` emitted by `buildRepoInventory` (`mcp/lib/repo-target.js:751`); consumers inert at `dashboard.js:186-189`, `assignment-brief.js:140-141`, `wave-brief-derivation.js:133-134`. Surfaced by the Plane-Δ verification pass (wf_e567e87f-926); recover from blobs `d1647c0`+`1a456f1`. Restore tracked as Δ-node I9. Not independently re-run — verifier-trusted per WEAVE confidence note.` Keep the honesty hedge: the I9 producer-drop was verified by the per-kind verifiers, not re-run line-by-line this session.
5. **Ship.** One commit, message e.g. `docs: correct stale I6 (deleted c02179c) + record I9 producer drop`. No code, no tests, no generators touched.

## REVIEW (engineering)
- **Tests:** doc-only change; run `npm run test:prompts` (covers `test/prompt-contracts.test.js`). The only assertion coupling the doc is `prompt-contracts.test.js:1365-1373` ("public docs do not advertise removed hook authority"), a *negative* match against `/hook-required/` and `/scope guard around/` — the new text must not introduce either string (trivially satisfied). No test asserts I6 prose, so no test edit is required; confirm full `npm test` stays green.
- **Context-budget compliance:** N/A — no brief, no subagent, no token cap introduced (budget = 0). The doc's own per-subagent caps elsewhere are untouched.
- **Determinism / content-hashing:** N/A for prose, but slice 2 must preserve the **append-only, newest-first** log contract (`:549`) — annotate, never reorder or delete historical entries.
- **Failure-mode coverage:** orphan — verify `mergePriorClaimMatches` is still test-only before calling it "orphaned" (re-grep `mcp/`,`prompts/` for callers); partial — slice 3 must not leave `:360/:363/:410/:424` contradicting the corrected `:194-206`; malformed — keep the doc's markdown header structure intact so `grep -n '^### '` still maps every node.
- **Slab invariants preserved:** cite `:57` ("any capability that violates a slab property … is a regression even if it works in demo") and `:602` ("Slab invariant — a property the substrate guarantees; violations are regressions even if they look like features"). The fix is itself an application of these: I6/I9 are documented regressions, so the doc must label them as such, not paper over them. Also honor anti-pattern #4 "Index without consumer" (`:536`) — the corrected I6 text must state plainly that the surviving prior primitive has no live consumer.

## REVIEW (parishioner / real-target gate)
Honestly engineering/fixture-only — there is **no real-target outcome** for a documentation correction. The closing gate is: (1) a fresh `grep "findings-index\|bob_index_candidate_claim\|priors_slice" docs/capability-hypergraph.md` returns only retracted/historical-marked hits (no live-tense claim); (2) the new DOC-2 log entry's file:line citations resolve against HEAD (`repo-target.js:751` has no `reachability`; `dashboard.js:186-189` still reads it); (3) `npm run test:prompts` green. This node does not move any tier's parishioner gate and must not be counted as capability progress.

## Context budget
Zero. No evaluator brief, no subagent, no MCP brief slice is created or read. If a future variant routes this through an agent (unnecessary), cap any such doc-edit subagent at ≤4k tokens (doc section excerpt in, diff out) — but the recommended path is a direct operator/maintainer `Edit`.

## Risks & doctrine compliance
- **Anti-pattern self-check:** *index-without-consumer* (`:536`) is the exact disease being documented, not committed — the fix names the orphaned `mergePriorClaimMatches` rather than re-asserting a consumer. No *substrate-as-trajectory*, *context-as-strategy*, or *auto-kill / freeze-then-verify inversion* surface — this introduces no code, no termination gate, no brief widening.
- **MCP-ownership / Write-policy compliance:** `docs/capability-hypergraph.md` is **not** an audit-graded path — `AUDIT_GRADED_PATHS` (`mcp/lib/paths.js:419,486`) covers only session-scoped artifacts (`report.md` `:381`, `chains.md` `:382`, joined with `sessionDir(domain)`). A repo doc is freely `Edit`-able; this does not collide with the reporting-rule "Do not manually edit Bob session artifacts" (that rule scopes to MCP-owned session JSON/JSONL/report prose, not `docs/`).
- **Append-only history risk:** the largest doctrinal trap is rewriting the `:570-572` log entries instead of annotating them — that would violate the "Append-only, newest first" contract at `:549`. Slice 2 must retract-in-place.
- **Honesty hedge (per WEAVE confidence note):** DOC-1 facts are code-verified this session (deletion, absent module, orphaned primitive, gone `calibration_label`). The I9 producer-drop in DOC-2 is verifier-trusted (per-kind verifiers), not independently re-executed — the log entry must say so, so the doc doesn't overclaim its own correction.
- **Frontier safety/scope gate:** N/A — not a frontier node, no network, no target interaction, no PII surface (Plane-O `--network none` and the no-PII hard rule are irrelevant to an offline doc edit).