"use strict";

// Repo-plane compatibility surface. The verifier contract lives in core so
// generic claim, invariant, and proof consumers do not depend on a repo runtime.
module.exports = require("../../core/repro-replay-verifier.js");
